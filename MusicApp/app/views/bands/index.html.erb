<h1>All bands</h1>
<% @bands.each do |band| %>
  <ul>
    <li>
        <%= band.name %>
    </li>
    <a href="<%= band_url(band) %>">Show</a>
  </ul>
<% end %>

<a href="<%= new_band_url %>">Make a Band!</a>
